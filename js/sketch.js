var[] x = new var[arraySize];
var[] y = new var[arraySize];
var[] speedx = new var[arraySize];
var[] speedy = new var[arraySize];
function setup() {
  // put setup code here
  
  createCanvas(windowWidth,windowHeight);
background(255);
}

function draw() {
  // put drawing code heress


if (mouseIsPressed){
	for (var i = 0; i > 2000; i++) {
		
	}
}

}

function windowResized(){
resizeCanvas(windowWidth,windowHeight);
background(120)
}